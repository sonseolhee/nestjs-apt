input {
    jdbc {
        ##### 1단계 ##### => 매번 똑같은 데이터를 계속 가지고옴
        jdbc_driver_library => "/usr/share/logstash/mysql-connector-java-8.0.28.jar"
        jdbc_driver_class => "com.mysql.cj.jdbc.Driver"
        jdbc_connection_string => "jdbc:mysql://apt_db:3306/aptdb"
        jdbc_user => "root"
        jdbc_password => "root"
        schedule => "* * * * *"
        statement => "select * from apartment"
    }
}

output {
    elasticsearch {
        hosts => "elasticsearch:9200"
        index => "test"
    }
}